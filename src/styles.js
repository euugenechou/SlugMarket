import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
         sectionTitle: {
           fontSize: 24,
           fontWeight: "700",
           paddingHorizontal: 20
         },
         sideScroll: {
           height: 130,
           marginTop: 20
         },
         
    });